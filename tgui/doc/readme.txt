F:\pan2\android_rtmp\ffmpeg\msys\home\Administrator\TGUI-0.6-RC

FIXME:
方法默认值
WidgetBorders      (childWindowToCopy), 复制构造
m_BackgroundTexture = childWindowToCopy.m_BackgroundTexture; 复制赋值操作是副本而非赋值
赋值函数交换操作
Integer.parseInt异常捕获
颜色属性赋值
ChildWindow::destroy_()命名冲突
赋值赋值构造函数实现(=符号)
析构函数调用超类实现
atoi = Integer.parseInt
static_cast<unsigned int>
target.draw(m_TextureArrowUpNormal, states); = target.draw(m_TextureArrowUpNormal.getSprite(), states);
to_string = toString
string::substr => string::substring
.rbegin()
复制构造函数，值复制
if (m_Background.getFillColor() != Color.Transparent 这种比较不正确
asign=>assign
cbegin
property == ->.equals(property)
Vector2f比较赋值Slider2d

* TextBox

2015-04-19 创建工程
2015-08-24 完成Java接口移植
